============
Installation
============

At the command line::

    $ pip install [A[Bdev-work-tracker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv [A[Bdev-work-tracker
    $ pip install [A[Bdev-work-tracker
